name: Deploy Frontend to Droplet

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build
        env:
          NODE_ENV: production

      - name: Deploy to Droplet
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: 22
          script: |
            # Create backup of current deployment
            if [ -d "/var/www/ecotachostec-frontend" ]; then
              sudo cp -r /var/www/ecotachostec-frontend /var/www/ecotachostec-frontend.backup.$(date +%Y%m%d_%H%M%S)
            fi
            
            # Create directory if it doesn't exist
            sudo mkdir -p /var/www/ecotachostec-frontend
            
            # Set proper permissions
            sudo chown -R $USER:$USER /var/www/ecotachostec-frontend

      - name: Copy build files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: 22
          source: "dist/*"
          target: "/var/www/ecotachostec-frontend"
          strip_components: 1
          overwrite: true

      - name: Restart Nginx
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: 22
          script: |
            # Set proper permissions
            sudo chown -R www-data:www-data /var/www/ecotachostec-frontend
            sudo chmod -R 755 /var/www/ecotachostec-frontend
            
            # Test nginx configuration
            sudo nginx -t
            
            # Reload nginx
            sudo systemctl reload nginx
            
            echo "✅ Frontend deployed successfully!"

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: 22
          script: |
            # Check if nginx is running
            sudo systemctl status nginx --no-pager
            
            # Test the endpoint
            curl -I http://localhost/ || echo "⚠️ Warning: Could not verify frontend"
            
            echo "🚀 Deployment completed at $(date)"